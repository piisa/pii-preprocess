# Default resource config filename for loader classes
DEFAULT_CONFIG_LOADER = "loader.json"

# Config section name for the preprocessor loader
FMT_CONFIG_LOADER = "pii-preprocess:loader:v2"

# Group name for the entry point in a pii-preprocess plugin package
PII_PREPROCESS_PLUGIN_ID = 'pii_preprocess.plugins'
